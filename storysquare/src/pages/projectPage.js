import React from "react";
import PageTemplate from "../components/templateProjectPage";

const ProjectPage = (props) => {
  return (
        <PageTemplate>
        </PageTemplate>
  );
};

export default ProjectPage;